{
  "name": "finanspanel-functions",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint --ext .ts src"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@types/nodemailer": "^7.0.1",
    "date-fns": "^3.6.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1",
    "nodemailer": "^7.0.6"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "eslint": "^9.11.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
