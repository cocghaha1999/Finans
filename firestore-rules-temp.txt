rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Geçici - tüm authenticated kullanıcılara tam erişim
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}