import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.costikfinans.app',
  appName: 'CostikFinans - Kişisel Finans',
  webDir: '../out'
};

export default config;
